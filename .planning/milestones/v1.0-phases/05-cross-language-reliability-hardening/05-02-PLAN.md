---
phase: 05-cross-language-reliability-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/error_engine.py
  - src/multi_error_detector.py
  - tests/test_c_java_js_regressions.py
autonomous: true
requirements:
  - DET-02
  - DET-03
  - DET-04
  - DIAG-01
  - DIAG-02
  - QA-01
must_haves:
  truths:
    - "C, Java, and JavaScript still produce useful rule-based findings when ML is unavailable."
    - "Single-result and multi-result outputs agree on issue types, ordering, and localization."
    - "String and comment characters do not trigger bracket or delimiter noise."
  artifacts:
    - path: "src/error_engine.py"
      provides: "Authoritative C/Java/JavaScript rule collection and localization"
      contains: "_collect_c_like_rule_based_issues"
    - path: "src/multi_error_detector.py"
      provides: "Grouped multi-error projection over the shared rule list"
      contains: "detect_all_errors"
    - path: "tests/test_c_java_js_regressions.py"
      provides: "Cross-language regression coverage for QA-discovered cases"
      contains: "test_multi_error_detector"
  key_links:
    - from: "src/error_engine.py"
      to: "src/multi_error_detector.py"
      via: "_collect_c_like_rule_based_issues()"
      pattern: "_collect_c_like_rule_based_issues"
    - from: "src/error_engine.py"
      to: "tests/test_c_java_js_regressions.py"
      via: "localized rule-based regressions"
      pattern: "line|col"
---

<objective>
Harden the shared non-Python detection contract in `src/` so fallback accuracy and localization remain trustworthy.

Purpose: Keep C, Java, and JavaScript detection centered in the shared engine per D-02, D-03, and D-04, not in entry-point forks.
Output: Refined shared rule logic plus regression coverage for canonical QA-discovered failures and negative cases.
</objective>

<execution_context>
@C:/Users/Asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cross-language-reliability-hardening/05-CONTEXT.md
@.planning/phases/05-cross-language-reliability-hardening/05-RESEARCH.md
@docs/QA_REPORT_2026-03-27.md
@src/error_engine.py
@src/multi_error_detector.py
@tests/test_c_java_js_regressions.py

<interfaces>
From src/error_engine.py:
```python
def _make_issue(...) -> dict:
    return {
        "type": issue_type,
        "message": message,
        "line": line,
        "col": col,
        "snippet": snippet,
        "suggestion": suggestion,
    }

def detect_errors(...) -> dict:
    return {
        "language": language,
        "predicted_error": primary_type,
        "confidence": 1.0,
        "tutor": explain_error(primary_type),
        "rule_based_issues": rule_based_issues,
        "degraded_mode": ...,
        "warnings": ...,
    }
```

From src/multi_error_detector.py:
```python
def detect_all_errors(...) -> dict:
    return {
        "language": language,
        "errors": all_errors,
        "total_errors": sum(error["count"] for error in all_errors),
        "has_errors": len(all_errors) > 0,
        "rule_based_issues": rule_based_issues,
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten authoritative C/Java/JavaScript rule collection and localization</name>
  <read_first>
    - src/error_engine.py
    - tests/test_c_java_js_regressions.py
    - docs/QA_REPORT_2026-03-27.md
    - .planning/phases/05-cross-language-reliability-hardening/05-RESEARCH.md
  </read_first>
  <files>src/error_engine.py, tests/test_c_java_js_regressions.py</files>
  <behavior>
    - Representative C, Java, and JavaScript failures stay detectable without ML.
    - Rule-based findings expose `line` and `col` whenever the helper can localize the issue.
    - String/comment masking and cascading-noise suppression prevent bracket or delimiter false positives.
  </behavior>
  <action>Use the QA report cases as the canonical regression set per D-03 and D-08, with C fixes prioritized first as suggested by the phase context. Refine `src/error_engine.py` only through the shared helper path: keep `_strip_c_like_comments_and_strings()` and `_suppress_cascading_syntax_noise()` on the hot path, preserve normalized issue keys, and improve localized `col` population for the rule families touched by this phase. Add or update targeted assertions in `tests/test_c_java_js_regressions.py` before finalizing the helper changes.</action>
  <verify>
    <automated>python -m pytest tests/test_c_java_js_regressions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "_strip_c_like_comments_and_strings|_suppress_cascading_syntax_noise|_collect_c_like_rule_based_issues|col" src/error_engine.py` shows the shared masking plus localization path remains intact.
    - `python -m pytest tests/test_c_java_js_regressions.py -q` passes with explicit C, Java, JavaScript, and negative string-literal regressions.
  </acceptance_criteria>
  <done>`detect_errors()` remains the authoritative source for C/Java/JavaScript fallback behavior, and the covered QA cases include stable line plus column metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep multi-error output a projection over the same shared rule results</name>
  <read_first>
    - src/multi_error_detector.py
    - src/error_engine.py
    - tests/test_c_java_js_regressions.py
    - docs/QA_REPORT_2026-03-27.md
  </read_first>
  <files>src/multi_error_detector.py, tests/test_c_java_js_regressions.py</files>
  <behavior>
    - Mixed snippets return multiple grouped findings instead of only the first issue.
    - Grouped output preserves line and column data from the shared rule issues.
    - ML-only additions do not duplicate a rule issue that was already found.
  </behavior>
  <action>Refine `src/multi_error_detector.py` per D-02 and D-06 so it stays a projection over `_collect_c_like_rule_based_issues()` rather than an alternate non-Python detector. Preserve grouped counts, location payloads, and tutor copy, and add ordering or dedupe regressions for the mixed-error QA samples already documented for Java and JavaScript.</action>
  <verify>
    <automated>python -m pytest tests/test_c_java_js_regressions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "_collect_c_like_rule_based_issues|locations|count|tutor" src/multi_error_detector.py` confirms grouped output still originates from the shared rule list.
    - `python -m pytest tests/test_c_java_js_regressions.py -q` passes with multi-error assertions for at least one Java and one JavaScript mixed-case snippet.
  </acceptance_criteria>
  <done>`detect_all_errors()` agrees with the single-result engine on issue identity and localization for the covered C/Java/JavaScript cases.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_c_java_js_regressions.py -q` after each task. Finish with a quick spot-check that the test file still covers C first, then JavaScript, then Java, matching the phase prioritization notes.
</verification>

<success_criteria>
DET-02, DET-03, DET-04, DIAG-01, DIAG-02, and QA-01 are enforced by shared `src/` logic plus regressions rather than entry-point-specific behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-language-reliability-hardening/05-02-SUMMARY.md`
</output>
