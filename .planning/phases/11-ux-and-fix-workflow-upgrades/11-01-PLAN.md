---
phase: 11-ux-and-fix-workflow-upgrades
plan: 01
type: execute
wave: 1
depends_on:
  - 08-01
  - 10-01
files_modified:
  - app.py
  - src/static_pipeline.py
  - src/auto_fix.py
  - tests/test_streamlit_parity.py
autonomous: true
requirements:
  - UX-01
  - UX-02
  - QUAL-01
---

<objective>
Make the Streamlit experience clearer and more trustworthy by reducing unnecessary recomputation, exposing better evidence, modernizing quality signals, and presenting validated fix previews with diffs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-ux-and-fix-workflow-upgrades/11-CONTEXT.md
@app.py
@src/static_pipeline.py
@src/auto_fix.py
@tests/test_streamlit_parity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce unnecessary Streamlit recomputation and improve analysis clarity</name>
  <files>app.py, src/static_pipeline.py, tests/test_streamlit_parity.py</files>
  <behavior>
    - Expensive analysis work is not repeated unnecessarily for unchanged code and settings.
    - Confidence, evidence, and degraded-mode signals are visible and understandable.
    - Existing parity expectations remain intact or are updated intentionally.
  </behavior>
  <action>Use Streamlit-friendly state and caching patterns to reduce noisy reruns, then expose the most important trust signals directly in the interface. Keep the app lightweight and student-readable.</action>
  <verify><automated>python -m pytest tests/test_streamlit_parity.py -q</automated></verify>
  <done>The UI feels more deliberate and informative without drifting from shared engine behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present validated fix previews with diffs</name>
  <files>app.py, src/auto_fix.py, tests/test_streamlit_parity.py</files>
  <behavior>
    - Fix previews show a clear before/after diff.
    - The UI distinguishes suggestion-only fixes from validated improvements.
    - Users do not see misleading success messaging for unverified or non-improving changes.
  </behavior>
  <action>Build the fix UX around verification truth, not optimistic copy. The interface should help students understand what changed, why it changed, and whether the change was actually validated.</action>
  <verify><automated>python -m pytest tests/test_streamlit_parity.py -q</automated></verify>
  <done>Fix previews are transparent and trustworthy enough to support student learning instead of hiding the underlying change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Modernize the quality analyzer for language-aware scoring</name>
  <files>src/quality_analyzer.py, app.py, tests/test_streamlit_parity.py</files>
  <behavior>
    - JavaScript comments and complexity rules are handled credibly.
    - Long-function and line-quality checks are not artificially Python/Java-only unless explicitly justified.
    - The surfaced quality score is more trustworthy for non-Python code.
  </behavior>
  <action>Improve the quality analyzer so it stops undermining trust when used on JavaScript and other non-Python snippets. Focus on language-aware comment handling, complexity semantics, and believable score components rather than inventing a full static-analysis suite.</action>
  <verify><automated>python -m pytest tests/test_streamlit_parity.py -q</automated></verify>
  <done>The quality score is no longer obviously naive for non-Python users.</done>
</task>

</tasks>

<verification>
- Streamlit avoids unnecessary reruns for repeated analysis actions.
- Evidence, confidence, and degraded-mode signals are surfaced clearly.
- Fix previews are diffed and validation-aware.
- Quality analysis is language-aware enough to support trust in the UI.
</verification>

<success_criteria>
- UX-01, UX-02, and QUAL-01 are satisfied without breaking parity with the shared engine.
- The UI communicates trust signals clearly enough for classroom use.
- Suggestion-only and validated-fix paths are visually distinct.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-and-fix-workflow-upgrades/11-01-SUMMARY.md`
</output>
