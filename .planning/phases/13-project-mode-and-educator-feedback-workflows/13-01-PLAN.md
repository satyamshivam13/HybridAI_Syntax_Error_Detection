---
phase: 13-project-mode-and-educator-feedback-workflows
plan: 01
type: execute
wave: 1
depends_on:
  - 12-01
files_modified:
  - api.py
  - app.py
  - cli.py
  - src/
  - docs/
autonomous: true
requirements:
  - PROD-03
  - PROD-04
  - EDU-01
---

<objective>
Push OmniSyntax beyond single-snippet usage by adding project-mode analysis, explicit user feedback capture, and lightweight educator review workflows built on the persistence foundation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/13-project-mode-and-educator-feedback-workflows/13-CONTEXT.md
@api.py
@app.py
@cli.py
@.planning/phases/12-product-foundations-and-commercial-readiness/12-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project-mode and multi-file analysis flow</name>
  <files>api.py, app.py, cli.py, src/</files>
  <behavior>
    - Users can submit multi-file or project-mode inputs.
    - The shared engine can reason across a bounded project context.
    - The feature builds on persisted analysis artifacts instead of bypassing them.
  </behavior>
  <action>Introduce a practical project-mode path that expands OmniSyntax beyond single snippets without turning the product into a full IDE. Keep the upload/input model simple and aligned across API, CLI, and Streamlit.</action>
  <verify><automated>python -m pytest tests/ -q</automated></verify>
  <done>Multi-file analysis is a real product capability rather than a future talking point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture explicit user feedback on analysis quality and fixes</name>
  <files>api.py, app.py, docs/</files>
  <behavior>
    - Users can mark whether an analysis or fix was useful.
    - Feedback is attached to persisted artifacts in a queryable way.
    - Reliability work can use these signals later.
  </behavior>
  <action>Add a lightweight feedback path that records usefulness and correction signals without overcomplicating the product. Make the data model explicit enough to support future ranking and improvement work.</action>
  <verify><automated>python scripts/check_links.py</automated></verify>
  <done>The product can learn from user feedback instead of relying only on synthetic regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add educator review and rubric-note workflows</name>
  <files>api.py, app.py, docs/</files>
  <behavior>
    - Educators can review saved analyses.
    - Rubric-style notes or feedback artifacts can be attached without needing a full dashboard product.
    - The workflow remains lightweight and artifact-centric.
  </behavior>
  <action>Implement the smallest useful educator review workflow on top of saved analyses. Keep it practical and review-oriented rather than pretending this phase ships complete classroom management.</action>
  <verify><automated>python scripts/check_links.py</automated></verify>
  <done>Teacher and rubric workflows are explicitly planned and implemented instead of living as vague future ambitions.</done>
</task>

</tasks>

<verification>
- Multi-file project-mode inputs can be analyzed.
- User feedback on analysis quality and fixes is captured explicitly.
- Educator review artifacts and rubric-style notes are supported.
</verification>

<success_criteria>
- PROD-03, PROD-04, and EDU-01 are satisfied with scoped, practical implementations.
- The repo now covers the remaining productization gaps identified in the April 19 audit.
- The phase extends the platform without overpromising a full classroom SaaS.
</success_criteria>

<output>
After completion, create `.planning/phases/13-project-mode-and-educator-feedback-workflows/13-01-SUMMARY.md`
</output>
