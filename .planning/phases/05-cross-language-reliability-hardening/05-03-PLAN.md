---
phase: 05-cross-language-reliability-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/tutor_explainer.py
  - api.py
  - cli.py
  - app.py
  - tests/test_api_and_regressions.py
  - tests/test_script_smoke.py
autonomous: true
requirements:
  - DIAG-03
  - DIAG-04
  - FIX-02
must_haves:
  truths:
    - "Detected issues include actionable tutor copy that says what is wrong and what to fix next."
    - "API, CLI, and Streamlit surface the same issue labels, locations, and degraded-mode warnings from shared payloads."
  artifacts:
    - path: "src/tutor_explainer.py"
      provides: "Shared explanation copy keyed by normalized error type"
      contains: "EXPLANATIONS"
    - path: "cli.py"
      provides: "Terminal rendering for warnings and localized issues"
      contains: "rule_based_issues"
    - path: "app.py"
      provides: "Streamlit rendering for warnings and localized issues"
      contains: "result['predicted_error']"
    - path: "tests/test_api_and_regressions.py"
      provides: "Shared payload and explanation regressions"
      contains: "tutor"
  key_links:
    - from: "src/tutor_explainer.py"
      to: "api.py"
      via: "detect_errors() payload"
      pattern: "tutor"
    - from: "src/tutor_explainer.py"
      to: "cli.py"
      via: "rendered predicted error and issue details"
      pattern: "predicted_error"
    - from: "src/tutor_explainer.py"
      to: "app.py"
      via: "Streamlit explanation section"
      pattern: "Why this happened"
---

<objective>
Align the student-facing contract across explanations, warnings, and entry-point wording.

Purpose: Keep DIAG-03, DIAG-04, and FIX-02 in shared contracts so API, CLI, and Streamlit render the same underlying result rather than drifting apart.
Output: More actionable tutor copy plus consistent API, CLI, and Streamlit rendering for warnings, labels, and locations.
</objective>

<execution_context>
@C:/Users/Asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-cross-language-reliability-hardening/05-CONTEXT.md
@.planning/phases/05-cross-language-reliability-hardening/05-RESEARCH.md
@docs/QA_REPORT_2026-03-27.md
@src/tutor_explainer.py
@api.py
@cli.py
@app.py
@tests/test_api_and_regressions.py
@tests/test_script_smoke.py

<interfaces>
From api.py:
```python
class ErrorResponse(BaseModel):
    language: str
    predicted_error: str
    confidence: float
    tutor: Dict[str, str]
    rule_based_issues: List[Dict[str, Any]]
    has_errors: bool
    degraded_mode: bool = False
    warnings: List[str] = Field(default_factory=list)
```

Current parity gaps to close:
- `cli.py` prints `predicted_error` and `rule_based_issues`, but does not render `warnings`.
- `app.py` renders tutor copy, but its detailed-issues header is Python-specific and it does not surface shared warnings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand shared tutor copy for the normalized non-Python issue set</name>
  <read_first>
    - src/tutor_explainer.py
    - docs/QA_REPORT_2026-03-27.md
    - .planning/phases/05-cross-language-reliability-hardening/05-RESEARCH.md
    - tests/test_api_and_regressions.py
  </read_first>
  <files>src/tutor_explainer.py, tests/test_api_and_regressions.py</files>
  <behavior>
    - Explanations for normalized C, Java, and JavaScript issue types are specific, actionable, and consistent across entry points.
    - Each explanation states both the cause and the next correction step.
  </behavior>
  <action>Update `src/tutor_explainer.py` per D-05 so the normalized issue types used in Phase 05 (`MissingDelimiter`, `UnmatchedBracket`, `UnclosedString`, `DivisionByZero`, `MissingInclude`, `MissingImport`, `TypeMismatch`, `UndeclaredIdentifier`, `InfiniteLoop`, `UnreachableCode`, `DuplicateDefinition`) have clear student-facing `why` and `fix` copy. Add regression assertions in `tests/test_api_and_regressions.py` that fail if those explanations fall back to generic text.</action>
  <verify>
    <automated>python -m pytest tests/test_api_and_regressions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"(MissingDelimiter|UnmatchedBracket|UnclosedString|DivisionByZero|MissingInclude|MissingImport|TypeMismatch|UndeclaredIdentifier|InfiniteLoop|UnreachableCode|DuplicateDefinition)\"" src/tutor_explainer.py` confirms explicit keyed copy exists for the normalized Phase 05 issue set.
    - `python -m pytest tests/test_api_and_regressions.py -q` passes with explanation-quality assertions.
  </acceptance_criteria>
  <done>Shared tutor explanations are no longer generic for the normalized C/Java/JavaScript issue types targeted by this phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface shared warnings and labels consistently in API, CLI, and Streamlit</name>
  <read_first>
    - api.py
    - cli.py
    - app.py
    - tests/test_api_and_regressions.py
    - tests/test_script_smoke.py
    - src/tutor_explainer.py
  </read_first>
  <files>api.py, cli.py, app.py, tests/test_api_and_regressions.py, tests/test_script_smoke.py</files>
  <behavior>
    - API responses keep returning `warnings`, `degraded_mode`, and localized `rule_based_issues`.
    - CLI output shows degraded-mode warnings and location metadata using the shared payload fields.
    - Streamlit shows the same issue label, warnings, and localized issue details without Python-only wording for non-Python cases.
  </behavior>
  <action>Keep the shared `detect_errors()` payload authoritative per D-07: do not invent alternate wording structures in the entry points. In `cli.py`, render `warnings` before the detailed issues section and preserve line plus column output. In `app.py`, surface `warnings`, remove the Python-specific detailed-issues heading for non-Python code, and render line plus column data from `rule_based_issues`. In `api.py`, preserve the current response-model contract while adding any parity-safe field wiring needed by the new tests. Back the changes with deterministic API and CLI coverage in `tests/test_api_and_regressions.py` and `tests/test_script_smoke.py`.</action>
  <verify>
    <automated>python -m pytest tests/test_api_and_regressions.py tests/test_script_smoke.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "warnings|degraded_mode|Column|rule_based_issues" api.py cli.py app.py` shows the shared payload fields are rendered in all three entry points.
    - `python -m pytest tests/test_api_and_regressions.py tests/test_script_smoke.py -q` passes.
  </acceptance_criteria>
  <done>API, CLI, and Streamlit all render the same shared result contract for issue labels, warnings, and localized findings.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_api_and_regressions.py tests/test_script_smoke.py -q` after each task. Spot-check that CLI and Streamlit wording now refers to localized issues generically rather than calling them Python-only syntax details.
</verification>

<success_criteria>
DIAG-03, DIAG-04, and FIX-02 are satisfied through shared explanations and shared payload rendering, not through duplicated entry-point-specific logic.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-language-reliability-hardening/05-03-SUMMARY.md`
</output>
