---
phase: 05-cross-language-reliability-hardening
plan: "04"
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - tests/test_script_smoke.py
  - README.md
  - docs/QA_REPORT_2026-03-27.md
  - .planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md
autonomous: false
requirements:
  - OPS-03
  - QA-02
  - QA-03
must_haves:
  truths:
    - "API, CLI, and Streamlit startup paths can be re-run with documented healthy and degraded expectations."
    - "Smoke coverage fails gracefully when ML artifacts are missing or unhealthy instead of crashing without context."
    - "Phase 05 sign-off evidence tells future contributors how to repeat the verification."
  artifacts:
    - path: "tests/test_script_smoke.py"
      provides: "Startup and graceful-degradation smoke coverage"
      contains: "subprocess.run"
    - path: "README.md"
      provides: "Documented local startup commands and runtime expectations"
      contains: "python start_api.py"
    - path: ".planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md"
      provides: "Phase-specific verification contract and manual sign-off steps"
      contains: "Streamlit"
  key_links:
    - from: "tests/test_script_smoke.py"
      to: "cli.py"
      via: "subprocess smoke"
      pattern: "cli.py"
    - from: "README.md"
      to: "start_api.py"
      via: "documented startup command"
      pattern: "start_api.py"
    - from: ".planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md"
      to: "app.py"
      via: "manual Streamlit sign-off checklist"
      pattern: "streamlit"
---

<objective>
Finish Phase 05 with repeatable smoke coverage and explicit sign-off evidence for all entry points.

Purpose: Lock startup and degraded-behavior verification into tests plus docs, then gather the one manual check the repo still cannot automate cleanly.
Output: Expanded smoke/startup checks, refreshed Phase 05 validation docs, and a blocking Streamlit verification checkpoint.
</objective>

<execution_context>
@C:/Users/Asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@docs/QA_REPORT_2026-03-27.md
@.planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md
@.planning/debug/resolved/ml-model-bundle-load-failure.md
@start_api.py
@api.py
@cli.py
@app.py
@tests/test_script_smoke.py

<interfaces>
Execution commands already blessed by AGENTS.md:
```bash
python -m pytest tests/ -q
python start_api.py
python -m streamlit run app.py
python cli.py tests/Test.java
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand startup and graceful-degradation smoke coverage, then document the sign-off path</name>
  <read_first>
    - tests/test_script_smoke.py
    - README.md
    - .planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md
    - docs/QA_REPORT_2026-03-27.md
    - .planning/debug/resolved/ml-model-bundle-load-failure.md
    - start_api.py
    - cli.py
    - app.py
  </read_first>
  <files>tests/test_script_smoke.py, README.md, docs/QA_REPORT_2026-03-27.md, .planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md</files>
  <behavior>
    - Smoke checks cover the CLI path and at least bounded startup sanity for API and Streamlit.
    - Scripts and entry-point smoke checks fail with readable diagnostics when ML state is forced unavailable.
    - The docs tell future contributors which commands prove healthy mode and which checks prove degraded-mode safety.
  </behavior>
  <action>Add deterministic smoke coverage per D-07 for `python cli.py tests/Test.java`, API startup/import sanity, and a bounded headless Streamlit startup check if it can be made stable within the existing tooling. Where full degraded startup cannot be executed directly, simulate the unavailable-model condition inside the smoke test and assert readable failure or warning output instead of a crash, satisfying QA-02. Update `README.md`, `docs/QA_REPORT_2026-03-27.md`, and `05-VALIDATION.md` with the exact startup commands, healthy/degraded expectations, and the remaining manual Streamlit review steps.</action>
  <verify>
    <automated>python -m pytest tests/test_script_smoke.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_script_smoke.py -q` passes.
    - `rg -n "python start_api.py|python -m streamlit run app.py|python cli.py tests/Test.java|healthy|degraded" README.md docs/QA_REPORT_2026-03-27.md .planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md` finds the updated sign-off guidance.
  </acceptance_criteria>
  <done>OPS-03, QA-02, and the doc side of QA-03 have repeatable commands plus current sign-off guidance tied to the real runtime state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Streamlit parity against API and CLI behavior</name>
  <read_first>
    - .planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md
    - README.md
    - app.py
    - api.py
    - cli.py
  </read_first>
  <files>.planning/phases/05-cross-language-reliability-hardening/05-VALIDATION.md, README.md</files>
  <action>Use the checklist written in Task 1 to verify D-07 end-to-end parity after the automated smoke coverage is green. Compare the same representative snippets across API, CLI, and Streamlit, and stop the phase if Streamlit still drops warnings, issue labels, or location details that the shared payload already provides.</action>
  <verify>
    <automated>python -m pytest tests/test_script_smoke.py -q</automated>
  </verify>
  <acceptance_criteria>
    - User confirms the Streamlit UI shows the same issue label, warning state, and location details as API plus CLI for the checklist snippets.
    - Any mismatch is written down as a concrete issue before the phase resumes.
  </acceptance_criteria>
  <what-built>Updated smoke coverage, startup documentation, and a Phase 05 checklist for API, CLI, and Streamlit parity.</what-built>
  <how-to-verify>
    1. Run `python start_api.py`, then open `http://localhost:8000/health` and confirm the status matches the healthy/degraded expectation documented in `README.md`.
    2. Run `python cli.py tests/Test.java` and confirm the CLI shows the same issue labels, warnings, and location fields described in the Phase 05 checklist.
    3. Run `python -m streamlit run app.py`, submit the representative C, Java, and JavaScript snippets listed in `05-VALIDATION.md`, and confirm the UI shows the same primary label, warning state, and localized issue details as API plus CLI.
  </how-to-verify>
  <resume-signal>Type `approved` if the Streamlit parity check passes, or reply with the exact mismatch you observed.</resume-signal>
  <done>The Streamlit checklist is either approved explicitly or converted into a concrete follow-up issue with the mismatch documented.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_script_smoke.py -q` after Task 1, then complete the human verification gate before closing the phase. If Streamlit diverges from API or CLI, stop and create a follow-up fix instead of hand-waving the mismatch.
</verification>

<success_criteria>
OPS-03, QA-02, and QA-03 have repeatable startup plus smoke evidence, and the remaining non-automated Streamlit parity check is either approved or turned into a concrete follow-up issue.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-language-reliability-hardening/05-04-SUMMARY.md`
</output>
