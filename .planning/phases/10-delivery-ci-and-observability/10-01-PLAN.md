---
phase: 10-delivery-ci-and-observability
plan: 01
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - .github/workflows/ci.yml
  - requirements.txt
  - requirements-dev.txt
  - Dockerfile
  - docker-compose.yml
  - api.py
  - start_api.py
  - README.md
  - .env.template
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - PERF-01
  - PERF-02
  - PERF-03
---

<objective>
Establish a real delivery and operations baseline with containers, stricter CI quality gates, structured visibility into API and engine behavior, and runtime scale-readiness primitives.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-delivery-ci-and-observability/10-CONTEXT.md
@.github/workflows/ci.yml
@requirements.txt
@requirements-dev.txt
@api.py
@start_api.py
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship a deployable runtime baseline and clean dependency hygiene</name>
  <files>Dockerfile, docker-compose.yml, requirements.txt, requirements-dev.txt, start_api.py, README.md, .env.template</files>
  <behavior>
    - The repo has a production-friendly container and startup path.
    - Production dependencies are separated cleanly from development-only tooling.
    - Local, staging, and production runtime expectations are documented.
    - Dependency and docs contracts match the live runtime bundle requirements.
  </behavior>
  <action>Introduce the minimal delivery artifacts needed to run OmniSyntax predictably outside a developer laptop. Clean up dependency boundaries so the runtime image is smaller and easier to reason about, and reconcile docs/runtime mismatches such as stale dependency expectations.</action>
  <verify><automated>python -m pytest tests/test_script_smoke.py -q</automated></verify>
  <done>The repo can be built and run as a real deployable artifact with documented configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn CI into a meaningful release gate</name>
  <files>.github/workflows/ci.yml, requirements-dev.txt, README.md</files>
  <behavior>
    - CI runs linting, typing, tests, coverage, and dependency/security checks.
    - Supported Python versions are exercised intentionally.
    - Failures point to actionable gaps instead of a single giant test step.
    - Performance and load-smoke checks have an explicit place in the gate.
  </behavior>
  <action>Expand CI beyond basic pytest and link checks so production regressions are harder to merge. Keep the gate realistic for the repo, but remove the current "tests passed therefore production-ready" illusion.</action>
  <verify><automated>python -m pytest tests/ -q</automated></verify>
  <done>CI meaningfully enforces quality and environment compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add structured logs and baseline metrics</name>
  <files>api.py, start_api.py, README.md</files>
  <behavior>
    - API requests, failures, and degraded-mode paths emit structured logs.
    - Baseline metrics exist for request volume, latency, and degraded-mode frequency.
    - Tracing and alert-friendly hooks exist for the same signals.
    - Operational documentation explains how to interpret the emitted signals.
  </behavior>
  <action>Add lightweight observability without over-engineering the repo. The goal is enough signal for incident review and performance regression tracking, not an enterprise telemetry platform on day one.</action>
  <verify><automated>python -m pytest tests/test_api_and_regressions.py -q</automated></verify>
  <done>Operators can tell what the service is doing, failing, and degrading on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add runtime scale-readiness seams</name>
  <files>api.py, start_api.py, README.md, .github/workflows/ci.yml</files>
  <behavior>
    - CPU-heavy analysis can be routed through a configurable worker or isolation seam.
    - Engine prewarm and cache strategy is explicit and documented.
    - Load and performance smoke checks are runnable and versioned.
  </behavior>
  <action>Design a pragmatic scale-readiness path without forcing a full distributed architecture. Add the seams and verification so later growth does not require ripping apart the request path under pressure.</action>
  <verify><automated>python -m pytest tests/test_script_smoke.py -q</automated></verify>
  <done>The platform has a believable path from demo traffic to moderate production load.</done>
</task>

</tasks>

<verification>
- Container and environment artifacts exist and are documented.
- CI enforces meaningful quality gates across the intended support matrix.
- API and engine behaviors produce structured operational signals.
- Worker/isolation, cache/prewarm, and load/perf smoke seams are present.
</verification>

<success_criteria>
- OPS-01, OPS-02, OPS-03, OPS-04, PERF-01, PERF-02, and PERF-03 are satisfied with pragmatic tooling choices.
- Delivery artifacts reflect the real runtime, not just local development shortcuts.
- Observability covers both API boundary issues and engine health/degradation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-delivery-ci-and-observability/10-01-SUMMARY.md`
</output>
