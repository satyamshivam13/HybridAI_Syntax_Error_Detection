---
phase: 06-python-and-java-reliability-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auto_fix.py
  - src/tutor_explainer.py
  - tests/test_detection.py
  - tests/test_error_and_repair_matrix.py
autonomous: true
requirements:
  - FIX-01
  - FIX-02
must_haves:
  truths:
    - "IndentationError and UnclosedString remain suggestion-only in v1.1 rather than being auto-rewritten."
    - "The guidance for both fixes includes exact location-aware context and clearer wording."
    - "The repaired snippets are asserted as unchanged when the suggestion-only path is exercised."
  artifacts:
    - path: "src/auto_fix.py"
      provides: "Suggestion-only behavior for indentation and unclosed strings"
      min_lines: 40
    - path: "src/tutor_explainer.py"
      provides: "Clearer tutor wording for the repaired fix types"
      min_lines: 20
    - path: "tests/test_detection.py"
      provides: "Unit coverage for suggestion-only fix behavior"
      min_lines: 20
    - path: "tests/test_error_and_repair_matrix.py"
      provides: "Regression coverage for unchanged repaired snippets"
      min_lines: 20
  key_links:
    - from: "src/auto_fix.py"
      to: "IndentationError / UnclosedString branches"
      via: "suggestion-only fix path with location-aware guidance"
      pattern: "IndentationError|UnclosedString"
    - from: "src/tutor_explainer.py"
      to: "user-facing fix wording"
      via: "clearer next-step guidance for the repaired cases"
      pattern: "IndentationError|UnclosedString"
    - from: "tests/test_detection.py"
      to: "apply_fixes"
      via: "assert unchanged code plus precise suggestion messaging"
      pattern: "apply_fixes"
---

<objective>
Keep IndentationError and UnclosedString fixes conservative while making the guidance precise enough to point to the correct location and explain the next correction clearly.

Purpose: Phase 6 should reduce confusion without turning these fixes into aggressive rewrites.
Output: Updated autofix and tutor wording plus regression tests that prove the suggestion-only behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-python-and-java-reliability-refinement/06-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@src/auto_fix.py
@src/tutor_explainer.py
@tests/test_detection.py
@tests/test_error_and_repair_matrix.py

<interfaces>
From src/auto_fix.py:
```python
class AutoFixer:
    def apply_fixes(self, code: str, error_type: str, line_num: int = None, language: str = None) -> dict:
        ...
```

From src/tutor_explainer.py:
```python
def explain_error(error_type: str) -> dict[str, str]:
    ...
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user code -> auto-fix suggestions | Untrusted snippets feed fix generation and should not silently mutate code for the locked cases |
| error explanation -> student-facing guidance | Tutor text must stay clear and should not mislead users into believing a rewrite happened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | T | src/auto_fix.py | mitigate | Keep the locked fix paths suggestion-only and assert the original code is preserved in regression tests. |
| T-06-04 | I | src/tutor_explainer.py | accept | The phase only changes wording for existing error types; no new sensitive data or external surfaces are introduced. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make indentation and string fixes suggestion-only with clearer location-aware guidance</name>
  <files>src/auto_fix.py, src/tutor_explainer.py</files>
  <behavior>
    - IndentationError stays suggestion-only and does not rewrite the code.
    - UnclosedString stays suggestion-only and does not rewrite the code.
    - The returned guidance includes exact location context and clearer wording so students can find the broken line quickly.
  </behavior>
  <action>Update the locked autofix branches so they no longer perform aggressive rewrites for IndentationError and UnclosedString. Keep the behavior conservative, preserve the original source text, and improve the guidance text so it is location-aware and easier for students to apply manually.</action>
  <verify><automated>python -m pytest tests/test_detection.py -q</automated></verify>
  <done>The suggestion-only autofix paths preserve the input code and provide precise guidance for the locked error types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock suggestion-only autofix behavior in regression tests</name>
  <files>tests/test_detection.py, tests/test_error_and_repair_matrix.py</files>
  <behavior>
    - The locked cases are asserted as suggestion-only, not auto-rewritten.
    - The repaired snippets remain unchanged when the suggestion-only path is used.
    - The assertions cover the exact guidance text or location context that the phase decision requires.
  </behavior>
  <action>Extend the existing autofix-related regression tests so they prove the new conservative behavior for IndentationError and UnclosedString. Keep the snippets concrete and assert the unchanged source plus the improved guidance wording or location context.</action>
  <verify><automated>python -m pytest tests/test_error_and_repair_matrix.py -q</automated></verify>
  <done>Regression coverage locks the suggestion-only behavior and the location-aware guidance for both autofix cases.</done>
</task>

</tasks>

<verification>
- IndentationError and UnclosedString no longer auto-rewrite code in the locked Phase 6 behavior.
- The guidance for both cases points to the correct location with clearer wording.
- Existing repair matrix coverage continues to pass for the unaffected error types.
</verification>

<success_criteria>
- FIX-01 and FIX-02 are implemented as suggestion-only behaviors with more precise guidance.
- The phase does not regress other autofix cases or the shared detection paths.
- Regression tests prove the original snippets stay unchanged for the locked cases.
</success_criteria>

<output>
After completion, create `.planning/phases/06-python-and-java-reliability-refinement/06-02-SUMMARY.md`
</output>
