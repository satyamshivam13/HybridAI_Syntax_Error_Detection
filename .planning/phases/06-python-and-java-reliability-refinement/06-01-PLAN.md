---
phase: 06-python-and-java-reliability-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/error_engine.py
  - src/multi_error_detector.py
  - tests/test_multi_error_detector.py
  - tests/test_api_and_regressions.py
  - tests/test_script_smoke.py
  - tests/test_streamlit_parity.py
autonomous: true
requirements:
  - DET-01
  - DET-02
  - QA-01
must_haves:
  truths:
    - "Python all-errors output includes confident semantic ML findings when the code is AST-valid and ML is confident enough to contribute."
    - "Java mixed-error cases keep TypeMismatch primary and suppress UndeclaredIdentifier only when declaration/type context proves the identifier warning is false positive."
    - "API, CLI, and Streamlit all expose the repaired detection behavior consistently for the locked regression snippets."
  artifacts:
    - path: "src/multi_error_detector.py"
      provides: "Python all-errors semantic merge"
      min_lines: 40
    - path: "src/error_engine.py"
      provides: "Java mixed-error precedence and suppression"
      min_lines: 40
    - path: "tests/test_multi_error_detector.py"
      provides: "Unit coverage for grouped Python semantic results"
      min_lines: 20
    - path: "tests/test_api_and_regressions.py"
      provides: "FastAPI regression coverage for repaired detection cases"
      min_lines: 20
    - path: "tests/test_script_smoke.py"
      provides: "CLI parity regression for repaired detection cases"
      min_lines: 20
    - path: "tests/test_streamlit_parity.py"
      provides: "Streamlit parity regression for repaired detection cases"
      min_lines: 20
  key_links:
    - from: "src/multi_error_detector.py"
      to: "detect_error_ml"
      via: "semantic merge into grouped all-errors output"
      pattern: "detect_error_ml"
    - from: "src/error_engine.py"
      to: "TypeMismatch / UndeclaredIdentifier precedence"
      via: "strict contextual suppression logic"
      pattern: "UndeclaredIdentifier|TypeMismatch"
    - from: "shared detection output"
      to: "api.py, cli.py, app.py"
      via: "same result contract rendered through all entry points"
      pattern: "predicted_error|errors_by_type|warnings"
---

<objective>
Lock the Python semantic all-errors merge and the Java mixed-error suppression behavior so the repaired detection contract is stable across the shared engine and all user-facing entry points.

Purpose: Phase 6 closes the remaining correctness gaps without widening scope.
Output: Updated detection code plus regression coverage in unit, API, CLI, and Streamlit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-python-and-java-reliability-refinement/06-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@src/error_engine.py
@src/multi_error_detector.py
@tests/test_multi_error_detector.py
@tests/test_api_and_regressions.py
@tests/test_script_smoke.py
@tests/test_streamlit_parity.py

<interfaces>
From src/multi_error_detector.py:
```python
def detect_all_errors(code: str, filename: str | None = None) -> dict:
    ...
```

From src/error_engine.py:
```python
def detect_errors(code: str, filename: str | None = None, language_override: str | None = None):
    ...
```

From tests/test_api_and_regressions.py:
```python
def _load_api(monkeypatch: pytest.MonkeyPatch, rate_limit: str = "100"):
    ...
```

From tests/test_streamlit_parity.py:
```python
def _run_app(code: str):
    ...
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user code -> shared detection engine | Untrusted snippets cross into semantic merge and mixed-error suppression logic |
| shared result -> API/CLI/Streamlit renderers | Structured findings are rendered in different surfaces and must stay consistent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | src/error_engine.py / src/multi_error_detector.py | mitigate | Keep suppression strict and context-based; add regression tests that prove the false positive only disappears when the declaration evidence is present. |
| T-06-02 | D | shared detection pipeline | accept | Python ML inference already exists in the current runtime; the phase does not introduce new external calls or broaden the attack surface. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge Python semantic ML into all-errors and tighten Java suppression</name>
  <files>src/multi_error_detector.py, src/error_engine.py</files>
  <behavior>
    - Python all-errors on AST-valid code adds a separate grouped semantic ML result when confidence is high enough.
    - Rule-based findings remain present; the semantic result is not hidden behind a rules-only gate.
    - Java mixed-error cases keep TypeMismatch primary and suppress UndeclaredIdentifier only when declaration/type context proves it is a false positive.
  </behavior>
  <action>Update the shared detection logic to honor D-01, D-02, and D-06. Keep the public result schema stable, preserve degraded-mode warnings, and avoid introducing a new output shape that downstream entry points would need to learn. The change should be limited to the existing detection and grouping code paths in src/multi_error_detector.py and src/error_engine.py.</action>
  <verify><automated>python -m pytest tests/test_multi_error_detector.py -q</automated></verify>
  <done>Python all-errors includes the locked semantic result shape, and Java suppression only applies when the mixed-case context proves the identifier finding is false positive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock repaired behavior across API, CLI, and Streamlit</name>
  <files>tests/test_api_and_regressions.py, tests/test_script_smoke.py, tests/test_streamlit_parity.py</files>
  <behavior>
    - The API returns the same repaired detection results for the locked Python and Java snippets.
    - The CLI still prints the same detection labels and warnings for the repaired snippets.
    - Streamlit displays the same repaired labels and highlighted locations for the repaired snippets.
  </behavior>
  <action>Add regression tests that exercise the repaired Python semantic merge and Java mixed-error suppression through the existing API, CLI, and Streamlit surfaces. Use the current testing patterns in the repo, keep the snippets concrete, and assert exact labels, warning payloads, and highlighted locations where applicable.</action>
  <verify><automated>python -m pytest tests/test_api_and_regressions.py tests/test_script_smoke.py tests/test_streamlit_parity.py -q</automated></verify>
  <done>The repaired detection behavior is locked across the shared API, CLI, and Streamlit entry points with concrete regression tests.</done>
</task>

</tasks>

<verification>
- The Python all-errors path returns grouped semantic findings when ML is confident and the snippet is AST-valid.
- Java mixed-error cases only suppress UndeclaredIdentifier when context proves it is a false positive.
- API, CLI, and Streamlit tests all agree on the repaired detection results and warnings.
- Full regression suite remains green after the focused task-level checks.
</verification>

<success_criteria>
- DET-01 and DET-02 are implemented without changing the public result schema in a way that breaks existing entry points.
- QA-01 is satisfied by automated coverage in unit, API, CLI, and Streamlit tests.
- No unrelated detection regressions are introduced in existing C, Java, or JavaScript cases.
</success_criteria>

<output>
After completion, create `.planning/phases/06-python-and-java-reliability-refinement/06-01-SUMMARY.md`
</output>
