---
phase: 07-security-and-api-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api.py
  - src/config.py
  - src/auto_fix.py
  - tests/test_api_and_regressions.py
  - .env.template
  - docs/API_DOCUMENTATION.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - API-01
  - API-02
  - API-03
---

<objective>
Make the API safe enough to deploy behind a real boundary by adding explicit access control, production-ready health semantics, strict typed contracts, and revalidated fix success behavior.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-security-and-api-hardening/07-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@api.py
@src/config.py
@src/auto_fix.py
@tests/test_api_and_regressions.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deploy-safe API boundary controls and health semantics</name>
  <files>api.py, src/config.py, .env.template, docs/API_DOCUMENTATION.md</files>
  <behavior>
    - Non-local deployments require an explicit access-control path.
    - Rate limiting supports local in-memory mode and a shared-store-ready production mode such as Redis-backed throttling.
    - Interactive docs can be disabled in production-facing configurations.
    - Health semantics distinguish liveness, readiness, and ML capability/degraded state.
  </behavior>
  <action>Introduce simple, explicit access control and production-minded health/readiness behavior without turning the repo into a full auth platform. Keep local development ergonomics intact, but make unsafe public exposure a deliberate opt-in rather than the default. Make the resulting design gateway-friendly with API-key style controls, shared-store throttling seams, and production-safe docs defaults.</action>
  <verify><automated>python -m pytest tests/test_api_and_regressions.py -q</automated></verify>
  <done>The API can no longer be described as anonymously deployable, and operators have clear health signals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten endpoint contracts and combined-flow response modeling</name>
  <files>api.py, tests/test_api_and_regressions.py, docs/API_DOCUMENTATION.md</files>
  <behavior>
    - Request models validate supported languages and fix types explicitly.
    - `/check-and-fix` has a declared response model.
    - Error payloads stay structured and consistent.
  </behavior>
  <action>Replace loose endpoint typing with stricter request and response contracts. Preserve the current API surface where reasonable, but remove ambiguity around enums, combined responses, and malformed inputs.</action>
  <verify><automated>python -m pytest tests/test_api_and_regressions.py -q</automated></verify>
  <done>API clients can rely on documented and validated contracts across all current endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revalidate fixes before claiming success</name>
  <files>api.py, src/auto_fix.py, tests/test_api_and_regressions.py</files>
  <behavior>
    - `/fix` and `/check-and-fix` only report success when the proposed output passes shared-pipeline verification.
    - Invalid or non-improving fixes surface clear failure details instead of optimistic success flags.
  </behavior>
  <action>Use the existing shared engine as the source of truth for fix validation. The endpoint contract should distinguish "generated a suggestion" from "verified an improvement."</action>
  <verify><automated>python -m pytest tests/test_api_and_regressions.py -q</automated></verify>
  <done>Fix endpoints are trustworthy enough to support the later UX work in Phase 11.</done>
</task>

</tasks>

<verification>
- Access control is explicit and documented for non-local use.
- Health endpoints/reporting separate process liveness from ML readiness and degraded capability.
- API contracts are strict and combined flows are typed.
- Fix success is tied to revalidation, not wishful string mutation.
</verification>

<success_criteria>
- SEC-01, SEC-02, API-01, API-02, and API-03 are satisfied without regressing degraded-mode behavior.
- Existing API regressions still pass and new boundary behavior is covered by tests.
- Documentation and environment templates match the implemented runtime switches.
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-and-api-hardening/07-01-SUMMARY.md`
</output>
