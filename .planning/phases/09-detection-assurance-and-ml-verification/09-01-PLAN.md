---
phase: 09-detection-assurance-and-ml-verification
plan: 01
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - src/static_pipeline.py
  - src/ml_engine.py
  - scripts/production_validation.py
  - scripts/retrain_model.py
  - .github/workflows/ci.yml
  - tests/test_static_pipeline_validation.py
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
---

<objective>
Add stronger assurance around non-Python detection and ML readiness by creating compiler/LSP adapter seams, healthy-mode verification, and benchmarkable shared-engine baselines.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-detection-assurance-and-ml-verification/09-CONTEXT.md
@src/static_pipeline.py
@src/ml_engine.py
@models/bundle_metadata.json
@scripts/production_validation.py
@tests/test_static_pipeline_validation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compiler or LSP adapter seams for C-like languages</name>
  <files>src/static_pipeline.py, tests/test_static_pipeline_validation.py</files>
  <behavior>
    - Java, C, C++, and JavaScript all have an adapter seam for stronger validation.
    - The current heuristic pipeline remains available as the fallback path.
    - Adapter failures degrade safely and visibly.
  </behavior>
  <action>Create an extensible validation layer that can host compiler or LSP-backed checks without forcing an immediate rewrite. The result should be concrete enough that future integrations are a continuation, not a fresh design exercise.</action>
  <verify><automated>python -m pytest tests/test_static_pipeline_validation.py -q</automated></verify>
  <done>The engine has a real path beyond handwritten heuristics for the C-like languages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove healthy-mode ML compatibility and capture baselines</name>
  <files>src/ml_engine.py, scripts/production_validation.py, scripts/retrain_model.py, .github/workflows/ci.yml</files>
  <behavior>
    - CI or an attached verification job proves healthy-mode load for the pinned bundle.
    - Compatibility metadata is explicit and checked.
    - Accuracy and latency baseline outputs are recorded for future comparison.
  </behavior>
  <action>Close the current assurance gap where degraded mode is well-tested but healthy mode is mostly trust-based. Tie ML compatibility to deterministic checks and baseline reporting.</action>
  <verify><automated>python -m pytest tests/test_static_pipeline_validation.py -q</automated></verify>
  <done>Healthy-mode ML is no longer a hand-verified assumption, and engine baselines are measurable.</done>
</task>

</tasks>

<verification>
- Compiler/LSP adapter seams exist and degrade safely.
- Healthy-mode ML verification is automated.
- Accuracy and latency baselines are captured for the shared engine.
</verification>

<success_criteria>
- REL-01, REL-02, and REL-03 are satisfied without regressing degraded-mode correctness.
- Phase outputs make future fidelity work measurable instead of anecdotal.
- CI and validation scripts agree on the artifact health contract.
</success_criteria>

<output>
After completion, create `.planning/phases/09-detection-assurance-and-ml-verification/09-01-SUMMARY.md`
</output>
